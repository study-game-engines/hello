#version 430 core
layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 6) buffer AtomicCounter {
    uint vertexCount;
};

void main() {   
    vertexCount = 0;    
}
